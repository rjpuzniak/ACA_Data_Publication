Script extracting subgroups of fibers from single fiber bundle. Extraction can be based on ROIs, streamline length or streamline weights criteria.
A separate option allows analysis as in ACA data set.

Input: 	fiber bundle to be segmented
		segmentation parameters (ROIs / values)

Output:	separate fibre bundles fulfilling defined criteria 
		
